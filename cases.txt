
expr $? + $? | echo $?  // probably need to change in the child process



cd ---> $PWD update
export with redirects ,, also maybe some other builtins
norm -- >handle_pipe



   Execute commands with single quotes as arguments.
    Try empty arguments.
    Try environment variables, whitespaces, pipes, redirection in the single quotes.
    echo '$USER' must print "$USER".
    Nothing should be interpreted.

wc< cat pula -- child process probaly payth error 

